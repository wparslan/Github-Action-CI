name: PHP Composer

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: PHPCS Code Review
        # You may pin to the exact commit or the version.
        # uses: rtCamp/action-phpcs-code-review@606311bf3bbb47cd4eda7c2de64360761114eb7e
        uses: rtCamp/action-phpcs-code-review@v2.1.0
  
