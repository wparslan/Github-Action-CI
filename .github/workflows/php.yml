name: PHP Composer

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

   - name: PHP Code Style (phpcs)
      # You may pin to the exact commit or the version.
      # uses: chindit/actions-phpcs@ccda708ed581ebe13a66497cc47c07b8346af3b3
      uses: chindit/actions-phpcs@1.0.1
      with:
        # Folder to check code style
        dir: # optional, default is .
  
